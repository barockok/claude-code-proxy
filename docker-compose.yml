services:
  claude-proxy:
    build: .
    ports:
      - "42069:42069"
    volumes:
      - ~/.claude:/root/.claude
      - ~/.claude-code-proxy:/root/.claude-code-proxy
    environment:
      - CCP_LOG_LEVEL=info
