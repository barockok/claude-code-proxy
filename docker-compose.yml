services:
  claude-proxy:
    build: .
    ports:
      - "42069:42069"
    volumes:
      - ~/.claude-code-proxy:/root/.claude-code-proxy
      - ./config.yaml:/config.yaml:ro
    environment:
      - CCP_LOG_LEVEL=info
